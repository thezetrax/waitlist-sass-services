{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "test:http": "hurl --variable host=http://localhost:3000 --test ./tests/http",
    "db:info": "echo 'db:* for database related actions'",
    "db:generate": "bun --bun drizzle-kit generate",
    "db:pull": "bun --bun drizzle-kit pull",
    "db:migrate": "bun run src/db/migrate.ts",
    "db:seed": "bun run src/db/seed.ts",
    "db:clean": "rm -rf data/app.db drizzle/"
  },
  "dependencies": {
    "better-sqlite3": "nounder/bun-better-sqlite3",
    "drizzle-orm": "^0.45.1",
    "drizzle-zod": "^0.8.3",
    "elysia": "latest",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@orangeopensource/hurl": "^7.1.3",
    "bun-types": "latest",
    "drizzle-kit": "^0.31.9"
  },
  "module": "src/index.js",
  "trustedDependencies": [
    "@orangeopensource/hurl"
  ]
}
